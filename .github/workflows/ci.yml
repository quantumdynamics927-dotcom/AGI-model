name: CI

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.10', '3.11']
    steps:
      - uses: actions/checkout@v4
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; else pip install pytest requests; fi
      - name: Run tests
        run: pytest -q

  ipfs_publish:
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    needs: test
    steps:
      - uses: actions/checkout@v4
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests
      - name: Publish sample metadata to Pinata
        env:
          PINATA_JWT: ${{ secrets.PINATA_JWT }}
          PINATA_API_KEY: ${{ secrets.PINATA_API_KEY }}
          PINATA_API_SECRET: ${{ secrets.PINATA_API_SECRET }}
          # NFT metadata publishing step DISABLED (on hold)
          # run: |
          #   python - <<'PY'
          #   from pathlib import Path
          #   from integrations import ipfs_adapter
          #   p = Path('nft_metadata')
          #   if not p.exists():
          #       print('no metadata to publish')
          #       exit(0)
          #   for f in list(p.glob('*.nft.json'))[:3]:
          #       cid = ipfs_adapter.upload_file(f)
          #       print(f'published {f} -> {cid}')
          #   PY
